a = [ [ '1',
          '434',
          '90000000000'],
        [ '2',
          '442',
          '90000000000'],
        [ '1',
          '444',
          '90000000000'] ];

let loggedIn = 0;
let active = 0;
let now = 450;

a.forEach(function(e){
  console.log(e);
  if(parseInt(e[0])==1){
    loggedIn=parseInt(e[1]);
  } else if (parseInt(e[0])==2) {
    active += parseInt(e[1])-loggedIn;
    loggedIn=0
  }
});
if(loggedIn) {
  active += now - loggedIn
}

console.log(active, loggedIn);
